source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'pry'
gem 'require_all'
require 'nokogiri'
require "open-uri"
# Specify your gem's dependencies in npr_todays_news.gemspec
gemspec
